📌 CORE / HANDOFF — Archiwum 3.0 (MAIL → CASES)

Контекст:
Пользователь строит систему Archiwum 3.0 на Windows + Dropbox.
Цель — автоматическая маршрутизация документов (PDF и вложений из Gmail / iCloud) в CASES по категориям, без ручной работы.

✅ Текущее состояние (зафиксировано)
1️⃣ Структура

Root:
C:\Users\alimg\Dropbox\Archiwum 3.0

MAIL_RAW (источник писем):
00_INBOX\MAIL_RAW

CASES вход:
CASES\_INBOX\FILES

REVIEW (сомнительное):
CASES\_REVIEW

Логи маршрутизации:
00_INBOX\_ROUTER_LOGS

2️⃣ Рабочие скрипты (ОК)

gmail_export_attachments.py
✔️ Экспортирует вложения из Gmail
✔️ Работает, dry-run проверен

cases_route_files.ps1
✔️ Маршрутизирует PDF из CASES\_INBOX\FILES
✔️ Dry-run и боевой запуск работают
✔️ Файлы реально перемещаются

Log-lock был побочный эффект тестов → не критично

3️⃣ Важный зафиксированный факт (КЛЮЧЕВОЕ)

🔴 Если PDF имеет имя из цифр / хеша — маршрутизатор НЕ МОЖЕТ определить категорию
➡ такие файлы корректно попадают в CASES_REVIEW

Это ожидаемое поведение, не баг.

🎯 Что НЕ сделано (следующий логический этап)
❗ Нет интеллектуального распознавания содержимого PDF

Сейчас маршрутизация:

❌ НЕ читает текст внутри PDF

❌ НЕ использует OCR

❌ НЕ использует subject / sender при роутинге

🚀 Следующий шаг (Шаг 3 системы)

Добавить второй уровень обработки для CASES_REVIEW:

Варианты:

pdf_inspect — если PDF текстовый

pdf_ocr — если скан

На основе найденных слов (faktura, umowa, zus, bmw, kuchnia и т.д.)
→ автоматический перенос в:

CASES\01_CLIENT

CASES\02_FIRMA

CASES\03_CAR

⚙️ Ограничения пользователя (обязательно учитывать)

Пользователь часто пишет с телефона, ноутбук может лагать

Предпочтение:

✅ команды PowerShell / Python

❌ минимум ручных правок

Лучше 1 команда → 1 действие

Не плодить лишние варианты и теории

🧠 Принцип работы ассистента

Не объяснять заново архитектуру

Не предлагать пересобирать уже работающие блоки

Работать по шагам, фиксируя завершённые этапы

Новый код — только как дополнение, не ломая существующее

🟢 Статус блока

MAIL → CASES (базовый роутинг): ЗАВЕРШЁН
Можно безопасно начинать новый чат и продолжать со Шага 3 (распознавание содержимого).